import { declareComponent } from "@webflow/react";
import { ComponentExample } from "../component-example";

export default declareComponent(ComponentExample, {
  name: "Component Example",
  description: "A component to showcase examples of other components.",
  group: "UI Components",
});
